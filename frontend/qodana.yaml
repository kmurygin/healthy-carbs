version: "1.0"

profile:
  name: qodana.recommended

linter: jetbrains/qodana-js:2025.2

include:
  - name: Eslint

bootstrap: |
  corepack enable || true
  pnpm -v >/dev/null 2>&1 && pnpm install --frozen-lockfile || (npm ci || npm i)

failureConditions:
  severityThresholds:
    any: 10
    critical: 1
    high: 5
